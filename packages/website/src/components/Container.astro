---
export interface Props {
  size?: "narrow" | "wide";
}

const { size } = Astro.props;
---

<div
  class:list={[
    "container mx-auto px-4 md:px-6",
    { "max-w-4xl": size === "narrow" },
    { "max-w-7xl": size === "wide" },
  ]}
>
  <slot />
</div>
