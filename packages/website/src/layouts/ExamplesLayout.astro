---
import type { MarkdownLayoutProps } from "astro";
import Navbar from "../components/Navbar.astro";
import BaseLayout from "./BaseLayout.astro";

interface Frontmatter {
  title: string;
  description?: string;
}

const {
  frontmatter: { title, description },
} = Astro.props as MarkdownLayoutProps<Frontmatter>;
---

<BaseLayout {title} {description}>
  <Navbar />
  <div class="max-w-7xl w-full mx-auto flex flex-1 items-stretch">
    <div class="flex flex-1 w-full">
      <article
        class="relative pb-8 w-full justify-center max-w-full flex min-w-0"
      >
        <main class="max-w-4xl px-6 md:px-8 pt-4 min-w-0 w-full">
          {/* TODO replace with <slot /> */}
          <div class="py-16 md:py-32">
            <p
              class="px-4 py-3 text-center bg-orange-50 border border-orange-100 text-orange-800 rounded"
            >
              Section under construction <span
                role="img"
                aria-label="construction worker">👷‍♂️</span
              >
            </p>
          </div>
        </main>
      </article>
    </div>
  </div>
</BaseLayout>
